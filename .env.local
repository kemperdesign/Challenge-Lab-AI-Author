# This file is no longer required!
# AI provider settings are now configured through the Settings UI in the application.
# Simply run `npm run dev` and click the Settings button to configure your AI provider.
#
# However, you can optionally set a default API key here if desired:
# GEMINI_API_KEY=your_gemini_api_key_here
